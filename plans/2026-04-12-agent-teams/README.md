

The user should be able to define and modify the agent team.

The first few agents should already be pre-defined - the coach, the engineering lead and the product agent.

The edit Agent Team tab should be accessible from the Projects page. The user should be able to redefine the agent's avatar and core system prompt.

The agents should also be attached to the workflow steps - each workflow step should be assigned to a specific agent so we might have to update the workflow model.
